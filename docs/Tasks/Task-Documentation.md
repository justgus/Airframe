# Legacy Tasks Index (Redirect)

This former mutable Task index is retained only for compatibility. It is not a current-state authority and must not be edited to create, assign, or transition Tasks.

Use AgileCockpit or the deterministic canonical projections in [`../generated/Tasks/`](../generated/Tasks/) for current Task state. Historical Task archives remain under [`Verified/`](Verified/) and [`Closed/`](Closed/).
